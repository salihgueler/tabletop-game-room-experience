# Flutter Workshop App

This is the finished Flutter frontend used throughout the workshop. Module 01
adds a sibling `backend/` folder. Later modules only evolve
`backend/aws-blocks/index.ts`; the Flutter UI remains a thin typed client.

Run `flutter pub get`, then begin at `../../01-scaffold/README.md`.
